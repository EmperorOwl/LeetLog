type Problem = {
  number: number;
  title: string;
  difficulty: string;
  topic: string;
  lastAttempted: string;
  trick: string;
  solution: string;
  comments: string;
};

export default Problem;
